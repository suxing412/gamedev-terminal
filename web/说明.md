<!-- 这份文件是 tools/出说明书.js 生成的，不要手改。正本是 docs/模块.json。 -->

# web

浏览器里跑的那一半。只走 HTTP，不 require 任何服务端模块，不自算任何状态分组——分组是后端算好的字段。

- **可依赖**：（不依赖任何层）
- **禁**：require 服务端
- **已建 2 · 待建 4**

## 已建

- `壳.js` —— 单壳外框：顶况条（版本/健康/进度）、页签（由视图表生成）、页面区。渲染是纯函数出树，判据在 Node 判树；挂 只在浏览器把树变 DOM。没登记真页面的系统走留白页——渲染说明书里这一圈的定义，正本一变页面就变。
- `生产.js` —— 研发格的真页面：谁在等我（逾期/上呈标出）、按格计数、甘特（SVG，一单一行按格上色、临界描边、依赖、现在线）、在途、工单库。只画后端算好的，不自算分组；登记进壳的页面表替掉留白。

## 待建

- **对话** —— 群聊与私聊、在座栏。
- **观测** —— 监视、班次、事件流、存照。
- **文稿** —— 文稿台与编辑器。
- **席位** —— 席位页与在座栏：名册、协议两层、选模型、权限卡、未选模型时明说缺什么并给出下一步。

---

这份说明由 `docs/模块.json` 生成。改那一份，然后 `npm run 说明书`。
