<!-- 这份文件是 tools/出说明书.js 生成的，不要手改。正本是 docs/模块.json。 -->

# 编排

会卡死的那一层：取单器、巡检、引擎通道、模型会话、上下文装配。唯一允许长跑的层，因此每处外呼必须有超时与单飞锁。

- **可依赖**：内核 · 领域
- **禁**：（无）
- **已建 7 · 待建 7**

## 已建

- `适配器.js` —— runtime adapter：一份契约（工单+执行卷+提示词 进，证据包 出），逐家 harness 一个适配器把契约映射到它自己的运行时。路由：上级指定 > 上级协议默认harness > 优先序，都要过能力校验。进方带超时。适配器要薄；厚的部分是契约与一致性判据。
- `适配器-claude.js` —— Claude Agent SDK 那家。有 hooks.PreToolUse，写闸做成硬闸（deny 实跑证实拦下）。改动清单 git 自算：含未跟踪新文件、中文路径不转义、跑前拍基线只算这一次改的。到点 abort 退出记 timeout。续接归 编排/会话。
- `契约一致性.js` —— 每家适配器必须过的同一套判据：同一张夹具单，各家交回的证据包形状一致；带单比对时机判初检结论也一致。契约改一个字段，五家全红——把「五处要记得同步」变成「五处会自己红」。
- `装配器.js` —— 一状态一会话的上下文包。首版装三样：这张单、进项文件的内容（不是路径，执行者不找）、协议职责权限节；人格语气不进执行卷；交付段按性质写死「不许自判做完、不许改状态」。历史会话与记忆条目等 领域/记忆 建了再接，现在不假装有。
- `取单器.js` —— 研发环常驻段：收草稿（必经剥）→成单编闸序列→审批→按拍取单（判据③进项未过闸不派、战役不派、按优先级）→路由→执行卷→装配→跑→证据包→初检记闸→深检；打回自修、三振待处理、上呈/炸进待处理；产出由代码从改动写。依赖全注入，缺了炸。
- `会话.js` —— Claude Agent SDK 会话：两种用途两种装法——对话席带人格语气层，审（深检站/评审台）只读工具不带人格；续接带 resume；用量与会话id 收回来；到点 abort。执行工单不走这里，走适配器。
- `深检站.js` —— 闸序列第二道：模型判质量。只读会话看证据包（改动/diff/回执/日志尾）加工单正文，只出结论与理由，不改文件不改状态；解读失效方向是拦——没有「结论：通过」一律打回。diff 超上限截断。

## 待建

- **引擎** —— Unity 通道。外部命令方式调，不 require。要带编辑器租约——白天人在用同一个工程时抢锁是现成的暴露面。
- **巡检** —— 看门狗与后台循环。每一条都要能说出「它坏了会怎样」。
- **评审台** —— 异厂评审：各厂领一份立场卷、互不可见、独立出意见。两厂撞到同一落点是最强的信号。
- **情报** —— 抓源、去重、打分、入库。源挂了要说是哪个源挂了、连败几次，不许出一份空日报当正常。
- **适配器-codex** —— codex exec 那家。有 PreToolUse（Bash/apply_patch，exit 2 或 deny 实跑拦下）+ .rules 命令白名单，写闸能硬。本机 Windows 无头沙箱不可用、MCP 工具绕过沙箱已实证——写闸放 hook 层不放沙箱层。细节见 docs/harness四家对照。
- **适配器-hermes** —— Hermes Agent（NousResearch v0.21.0，MIT，Python）——是 harness 不是模型。三层 pre_tool_call、写路径硬拒、SQLite+FTS5 中文检索，兼知识库管理员候选。细节见对照表。
- **适配器-dsh** —— DeepSeek harness（0.1.2-alpha）。headless 只走 argv 无 stdin、无 usage、会话 zstd 不可直读、resume 只有 ACP、明写会破坏性变更。四家最弱，最后接。细节见对照表。

---

这份说明由 `docs/模块.json` 生成。改那一份，然后 `npm run 说明书`。
